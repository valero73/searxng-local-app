# Copy to .env or let start.sh / start.bat generate it.
# Never commit a real .env.

SEARXNG_VERSION=latest
SEARXNG_PORT=8888
SEARXNG_BASE_URL=http://127.0.0.1:8888/
SEARXNG_SECRET=change-me-on-first-run
